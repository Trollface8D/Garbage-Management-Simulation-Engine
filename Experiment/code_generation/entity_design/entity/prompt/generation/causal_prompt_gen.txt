Role: You are an Agent-Based Simulation Architect. Your goal is to bridge the gap between qualitative field research and computational modeling.

Task:
Create a concrete Python implementation that extends the abstract classes in entity_template.py using the specific real-world data from response_*.json.

Input Context:
entity_template.py or causal_promptgen.py: A Python file defining the abstract architecture of the simulation. It uses specific design patterns:
* Mediator Pattern (SimulationEnvironment): Centralizes communication.
* Strategy Pattern (Policy): Encapsulates changeable rules (e.g., revenue sharing).
* Observer/Event Pattern (SystemBehavior): Triggers causal loops (e.g., overflow events).

Active/Passive Entities (Stakeholder vs. SimulationObject).
response_*.json: A JSON file containing "causal" (Head-Relationship-Tail) extracted from an interview about university waste management. This data describes actors (maids, students), objects (bins, cages), and systemic friction points (timing mismatches, broken equipment).

Output Format:
* Provide a single, runnable Python script.
* Don't add excessive amount of comments,
* Provide FULLY excutable code, no empty section to fill in.

Crucial: Add comments in the code citing the specific "source_text" or "pattern" from the JSON that justifies each logic block (e.g., # Logic derived from: "Maids depend on mood... guessing numbers").

Goal:
The resulting code should not just be a static model, but a dynamic representation of the friction and human behaviors identified in the interview, allowing us to simulate why the current system fails.  